# 勋章系统测试指南

## 测试环境

- 前端: http://localhost:3000
- 后端: http://localhost:3001
- 测试账号:
  - 家长: `parent` / `parent123`
  - 儿童: `xiaoming` / `child123`

## 测试步骤

### 1. 测试儿童端勋章收藏页面

1. 访问 http://localhost:3000
2. 使用儿童账号登录: `xiaoming` / `child123`
3. 在仪表板点击"我的勋章"卡片（🏅图标）
4. 应该看到:
   - 勋章收集进度: 0/3
   - 三个勋章卡片:
     - 🌟 学习之星 - 67% 完成 (2/3)
     - 🏠 独立能手 - 33% 完成 (1/3)
     - ⚽ 运动之星 - 0% 完成 (0/3)
   - 筛选按钮: 全部(3) / 已获得(0) / 未获得(3)

**预期效果:**
- 渐变背景（蓝色-紫色-粉色）
- 勋章卡片显示进度条
- 未获得的勋章图标为灰色
- 悬停卡片时有放大效果

### 2. 测试家长端勋章管理页面

1. 退出登录
2. 使用家长账号登录: `parent` / `parent123`
3. 在仪表板点击"勋章管理"卡片（🏅图标）
4. 应该看到:
   - 三个系统预设勋章
   - 每个勋章显示图标、名称、描述、获得条件
   - 系统预设勋章有"⚙️ 系统预设"标记
   - "➕ 创建自定义勋章"按钮

**预期效果:**
- 渐变背景（紫色-粉色-红色）
- 勋章卡片整齐排列
- 系统预设勋章没有删除按钮

### 3. 测试创建自定义勋章

1. 在家长端勋章管理页面
2. 点击"创建自定义勋章"按钮
3. 填写表单:
   - 勋章名称: "阅读达人"
   - 勋章描述: "连续一周每天阅读30分钟"
   - 选择图标: 📚
   - 关联规则: 选择"完成作业"
   - 完成次数: 7
   - 时间周期: 每周
4. 点击"创建勋章"

**预期效果:**
- 模态框弹出
- 表单验证正常
- 创建成功后模态框关闭
- 新勋章出现在列表中
- 新勋章有删除按钮（🗑️）

### 4. 测试删除自定义勋章

1. 在刚创建的"阅读达人"勋章卡片上
2. 点击删除按钮（🗑️）
3. 确认删除

**预期效果:**
- 弹出确认对话框
- 确认后勋章从列表中消失
- 系统预设勋章的删除按钮点击后提示"系统预设勋章不能删除"

### 5. 测试勋章进度更新

1. 保持家长账号登录
2. 进入"积分操作"页面
3. 给小明添加积分:
   - 选择用户: xiaoming
   - 选择规则: "完成作业"
   - 积分: 10
   - 原因: "今天完成作业"
4. 提交

5. 退出登录，使用儿童账号登录
6. 进入"我的勋章"页面
7. 查看"学习之星"勋章

**预期效果:**
- 学习之星进度应该变为 100% (3/3)
- 勋章应该显示为"已获得"
- 图标变为彩色
- 显示获得时间

### 6. 测试筛选功能

1. 在儿童端勋章收藏页面
2. 点击"已获得"筛选按钮
3. 应该只显示已获得的勋章
4. 点击"未获得"筛选按钮
5. 应该只显示未获得的勋章
6. 点击"全部"筛选按钮
7. 应该显示所有勋章

**预期效果:**
- 筛选按钮高亮显示当前选中状态
- 勋章列表根据筛选条件动态更新
- 筛选按钮显示对应数量

### 7. 测试API端点

使用curl或Postman测试以下端点:

```bash
# 1. 登录获取token
curl -X POST http://localhost:3001/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"parent","password":"parent123"}'

# 2. 获取勋章模板
curl http://localhost:3001/api/badges/templates \
  -H "Authorization: Bearer {token}"

# 3. 获取用户勋章
curl http://localhost:3001/api/badges/user/{userId} \
  -H "Authorization: Bearer {token}"

# 4. 创建自定义勋章
curl -X POST http://localhost:3001/api/badges/templates \
  -H "Authorization: Bearer {token}" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "测试勋章",
    "description": "测试描述",
    "icon": "🎯",
    "category": "CUSTOM",
    "ruleId": "{规则ID}",
    "count": 3,
    "period": "week"
  }'

# 5. 删除自定义勋章
curl -X DELETE http://localhost:3001/api/badges/templates/{badgeId} \
  -H "Authorization: Bearer {token}"
```

## 常见问题排查

### 问题1: 勋章页面显示空白
- 检查服务器是否正常运行
- 检查浏览器控制台是否有错误
- 确认token是否有效

### 问题2: 进度不更新
- 确认积分记录已成功创建
- 检查规则ID是否正确
- 确认时间周期计算是否正确

### 问题3: 无法创建自定义勋章
- 确认使用家长账号登录
- 检查表单是否填写完整
- 确认规则ID存在且有效

### 问题4: 删除按钮不显示
- 系统预设勋章不显示删除按钮（正常）
- 只有自定义勋章才有删除按钮

## 测试检查清单

- [ ] 儿童端可以查看勋章收藏
- [ ] 勋章进度正确显示
- [ ] 筛选功能正常工作
- [ ] 家长端可以查看所有勋章
- [ ] 家长端可以创建自定义勋章
- [ ] 家长端可以删除自定义勋章
- [ ] 系统预设勋章不能删除
- [ ] 勋章自动颁发功能正常
- [ ] UI样式符合游戏化设计
- [ ] 所有API端点正常响应

## 性能测试

1. 创建10个自定义勋章，检查页面加载速度
2. 添加100条积分记录，检查进度计算速度
3. 快速切换筛选器，检查响应速度

## 浏览器兼容性测试

- [ ] Chrome
- [ ] Firefox
- [ ] Safari
- [ ] Edge

## 移动端测试

- [ ] 响应式布局正常
- [ ] 触摸操作流畅
- [ ] 字体大小适中
- [ ] 按钮易于点击
